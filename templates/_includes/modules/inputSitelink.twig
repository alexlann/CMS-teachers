<p class="w-full">
    <label class="font-bold">{{ "Site link"|t }}: </label>
    {{ input('text', 'fields[body]', block.body, {
        id: 'fields[body]',
        class: "input-project",
    }) }}
</p>